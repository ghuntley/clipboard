using System;

// var applicationContext = Application.Context;
// ClipboardManager clipboard = (ClipboardManager)applicationContext.GetSystemService(Context.ClipboardService);
// ClipData clip = ClipData.NewPlainText("someIdentifier",clipboardText);
// clipboard.PrimaryClip = clip;

namespace Clipboard.Droid
{
    public class Clipboard : IClipboard
    {
    }
}

